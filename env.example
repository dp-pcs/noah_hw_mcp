# School Portal Configuration
PORTAL_BASE_URL=https://portal.example.edu
LOGIN_URL=https://portal.example.edu/login  # Optional: specific login URL (overrides LOGIN_PATH)
LOGIN_PATH=/login  # Only used if LOGIN_URL is not set

# Credentials (use .env file or macOS Keychain)
PORTAL_USERNAME=your_username_here
PORTAL_PASSWORD=your_password_here

# Optional Configuration
STATE_PATH=./state.json
HEADLESS=true
